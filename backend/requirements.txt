fastapi>=0.111
uvicorn[standard]>=0.30
anthropic>=0.40.0
pydantic>=2.9
python-dotenv>=1.0
httpx>=0.27          # Ollama (local) provider talks to /api/chat over HTTP
browser-cookie3>=0.20 # optional: reuse a local browser's logged-in session for authenticated web scans
python-multipart>=0.0.9 # required by FastAPI to accept .zip source uploads (POST /scan/upload)
